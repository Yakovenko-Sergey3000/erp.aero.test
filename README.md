# 🔐 Сервер регистрации и авторизации пользователей (JWT + Fingerprint)

REST API сервер для регистрации и авторизации пользователей с использованием **JSON Web Token (JWT)** и уникального **отпечатка устройства (fingerprint)**.

## ⚙️ Основные возможности

- Регистрация и авторизация пользователей
- Генерация **access** и **refresh токенов**
- Проверка токенов и продление сессий
- Использование **fingerprint** для защиты от кражи refresh-токенов
- Возможность иметь несколько активных сессий с разных устройств
- Централизованная обработка ошибок
- Поддержка middleware для аутентификации и валидации

## 🧩 Как работает fingerprint

Каждое устройство или браузер получает уникальный **fingerprint**,  
который передаётся в заголовке `X-Fingerprint` при каждом запросе.

- При авторизации fingerprint сохраняется вместе с refresh-токеном.
- При обновлении токена сервер проверяет, совпадает ли fingerprint.  
  Если отпечаток не совпадает — токен считается недействительным.
- Это предотвращает использование украденных токенов с других устройств.

## 🛠️ Технологии

- **Node.js**, **Express**
- **TypeScript**
- **JWT (jsonwebtoken)**
- **bcrypt** — хеширование паролей
- **PostgreSQL**
- **Docker** (опционально)

##  Поднять базу в Docker

### Конфигурация (.env)
Проект использует переменные из `configs/config.js` и `docker-compose.yaml`. 
Создайте файл `.env` в корне проекта и скопируйте параметры из `.env.sample`.

Запустите команду:

```bash
# из корня репозитория
docker compose up -d
```

## 2) Установить зависимости

Установите зависимости через npm или yarn:

```bash
# npm
npm install

# или yarn
yarn install
```

## 3) Запустить сервер

Сервер автоматически применяет миграции при старте. Для запуска используйте:

```bash
# npm
npm run start

# или yarn
yarn start
```

По умолчанию сервер поднимется на порту, указанном в переменной `PORT` (если не задана — 3000).



## Проверка запуска
- После запуска Docker базы и сервера вы должны увидеть в логах:
  - `✅ Server is running on port <PORT>"`
- Доступ к API осуществляется по адресу: `http://localhost:<PORT>`.

## Postman collection
Для проверки api можно импортировать postman колекцию которая лежит в папке `/docs`

## Endpoints

| Метод    | Endpoint            | Описание                                                        |
| -------- | ------------------- | --------------------------------------------------------------- |
| **POST** | `/signup`           | Регистрация нового пользователя                                 |
| **POST** | `/signin`           | Авторизация пользователя и получение токенов                    |
| **POST** | `/logout`           | Завершение сессии пользователя (инвалидирует refresh-токен)     |
| **GET**  | `/info`             | Получение информации об авторизованном пользователе             |
| **POST** | `/signup/new-token` | Обновление access-токена с помощью refresh-токена и fingerprint |
